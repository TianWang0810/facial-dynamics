external/
data/
models/

# Pipeline output: one directory per run, regenerable from the inputs and the
# recorded revision. Never committed -- geometry.parquet alone is large.
runs/
*.npz
*.mp4
__pycache__/
*.pyc

# Personal notes — HPC-local only, never shared (see CLAUDE.md convention)
/CLAUDE.md
